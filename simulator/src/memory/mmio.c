

// static inline bool is_sram(uint64_t addr) {
//     return (addr >= SRAM_BASE && addr < (uint64_t)SRAM_BASE + SRAM_SIZE);
// }

// static inline bool is_mmio(uint64_t addr) {
//     return (addr >= 0x10000000L && addr < 0x40000000L);
// }

// static inline bool is_uart(uint64_t addr) {
//     return (addr >= UART_BASE && addr < UART_BASE + UART_SIZE);
// }